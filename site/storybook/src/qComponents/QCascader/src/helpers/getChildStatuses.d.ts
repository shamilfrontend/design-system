import { Option, QCascaderPropModelValue } from '../types';
declare const getChildStatuses: (
  option: Option,
  checkedValues: QCascaderPropModelValue
) => boolean[];
export default getChildStatuses;
