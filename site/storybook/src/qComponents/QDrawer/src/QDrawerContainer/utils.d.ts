import { QDrawerComponent } from '../types';
import { QDrawerContainerPropContent } from './types';
export declare const isExternalComponent: (
  object: QDrawerContainerPropContent
) => object is QDrawerComponent;
