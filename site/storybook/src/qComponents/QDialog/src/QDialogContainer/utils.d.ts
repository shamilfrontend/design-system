import { QDialogComponent } from '../types';
import { QDialogContainerPropContent } from './types';
export declare const isExternalComponent: (
  object: QDialogContainerPropContent
) => object is QDialogComponent;
